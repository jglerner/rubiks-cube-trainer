import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rubikscube.trainer',
  appName: 'Rubiks Cube Trainer',
  webDir: 'dist'
};

export default config;
